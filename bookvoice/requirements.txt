Flask>=2.0
Werkzeug>=2.0
easyocr>=1.4
pdfplumber>=0.5
python-docx>=0.8
googletrans>=3.0.0
pyttsx3>=2.90
pydub>=0.25.0

# Note: For MP3 merging, ffmpeg must be installed:
# Windows: choco install ffmpeg
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg